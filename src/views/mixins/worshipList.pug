mixin worshipBlock(worship={})
    li(data-id=worship.id)
        a(href=`worship/${worship.id}`)
            div
                h4=worship.title
            div
                span=worship.year
                span=worship.month 
                span=worship.date
